{
  "name": "nap",
  "version": "0.1.0",
  "description": "NAP — Napkin Agent Protocol",
  "main": "src/main/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "esbuild src/renderer/index.jsx --bundle --outdir=dist --jsx=automatic --platform=browser --target=chrome120",
    "postinstall": "electron-rebuild"
  },
  "dependencies": {
    "@xterm/xterm": "^5.0.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/addon-canvas": "^0.7.0",
    "@xterm/addon-fit": "^0.10.0",
    "node-pty": "^1.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "@electron/rebuild": "^3.6.0",
    "esbuild": "^0.24.0"
  }
}
