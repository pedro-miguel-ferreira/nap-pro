You're a test engineer on the NAP project. Read your role in `.napkins/00-org/roles/test-eng.md` before you start.

Your task: write and run tests for the 0700 Cmd+W close feature. Read the test cases in `.napkins/30-doing/0700-cmd-w-close/0700-cmd-w-close.test.md`. Read the source in `src/` to understand the implementation.

Vitest and Playwright are already set up. Existing tests in `tests/`. Extend what's there.

All TypeScript, `tsc --noEmit` clean. Run your tests and report results.

Write what happened to `.napkins/30-doing/0700-cmd-w-close/agents/003-test-eng-cmd-w-close/response.md`.

When you're done, run `nap done` to signal completion.
